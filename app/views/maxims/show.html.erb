<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @maxim.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @maxim.content %>
</p>

<%= link_to 'Edit', edit_maxim_path(@maxim) %> |
<%= link_to 'Back', maxims_path %>
