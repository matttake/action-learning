- if user_signed_in?
  .index.container-fluide
    .index__maxim
      %p= @maxim.text
      %p= @maxim.name
  .index__warapper.container-fluid
    -@posts.each do |post|
      = render partial:"action",locals:{post:post}
- else
  =render "landing"